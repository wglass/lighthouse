sentry-web
rabbitmq
riak
zookeeper <- not managed by lighthouse
sentry-worker
flower-web
postfix-mta
mail-proxies  <- fixed IPs
sentry-smtp
sentry-redis <- for update buffer and time-series
sentry-pg
influxdb
grafana-web
ghost-pg
ghost-web
logstash
elasticsearch
kibana-web


monitoring:

  diamond -> influxdb -> grafana

logs:

  syslog -> logstash -> elasticsearch -> kibana
                    \-> influxdb -> grafana


base roles:

  python stack
  jvm stack
  cluster node
  monitored
  logged
